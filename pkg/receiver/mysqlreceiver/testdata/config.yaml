# extensions:
#   sumologic/2:
#     collector_name: DB_INTEGRATOR_TEST_2
#     collector_category: collector_category
#     install_token: ************
#     api_base_url: https://stag-open-events.sumologic.net
# processors:
#   groupbyattrsprocessor:
#     keys:
#       - body
exporters:
  logging:
    loglevel: debug
    sampling_initial: 0
    sampling_thereafter: 1
  # sumologic:
  #   auth:
  #     authenticator: sumologic/2
  #   source_category: "DB_Collector"
  #   source_name: "http input"
  #   source_host: "mysqlRecords"
  #   compress_encoding: ""
  #   # metadata_attributes:
  #   #   - body
  #   sending_queue:
  #     enabled: true
  #   log_format: otlp
receivers:
  mysql:
    endpoint: localhost:3306
    transport: tcp
    username: Jayanta
    password: *******
    database: employees
    collection_interval: 10s
    db_queries:
      - queryid: "Q1"
        query: "Select * from departments;"
      #   index_column_name: DATE
      #   initial_index_column_start_value: '2022-05-05 10:16:50'
      #   index_column_type: TIMESTAMP
      # - queryid: "Q2"
      #   query: "Select * from Persons"
      #   index_column_name: PersonID
      #   initial_index_column_start_value: 2 
      #   index_column_type: INT
service:
  #extensions: [sumologic/2]
  pipelines:
    logs:
      exporters: [logging]
      # processors: [groupbyattrsprocessor]
      receivers: [mysql]