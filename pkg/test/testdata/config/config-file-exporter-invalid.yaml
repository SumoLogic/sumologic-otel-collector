receivers:
  filelog:
    include:
      - ./testdata/logs/test
    start_at: beginning

exporterss:
  file:
    path: ./output/test.log
    rotation:
      max_megabytes: 10
      max_days: 3
      max_backups: 3

service:
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [file]
