receivers:
  otlp:
    protocols:
      grpc:
      http:
exporters:
  sumologic:
processors:
  remotetap:
    endpoint: 0.0.0.0:12001
    limit: 1
service:
    pipelines:
      logs:
        receivers:
          - otlp
        processors:
          - remotetap
        exporters:
          - sumologic
