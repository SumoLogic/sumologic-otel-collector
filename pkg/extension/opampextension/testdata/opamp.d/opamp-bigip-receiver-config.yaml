receivers:
  bigip:
    collection_interval: 10s
    endpoint: https://localhost:443
    username: otelu
    password: password
    tls:
      insecure_skip_verify: true
exporters:
  sumologic:
service:
  pipelines:
    logs:
      receivers:
        - bigip
      exporters:
        - sumologic
