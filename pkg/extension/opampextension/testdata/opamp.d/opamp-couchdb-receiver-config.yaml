receivers:
  couchdb:
    endpoint: http://localhost:5984
    username: otelu
    password: ${env:COUCHDB_PASSWORD}
    collection_interval: 60s
exporters:
  sumologic:
service:
  pipelines:
    logs:
      receivers:
        - couchdb
      exporters:
        - sumologic
