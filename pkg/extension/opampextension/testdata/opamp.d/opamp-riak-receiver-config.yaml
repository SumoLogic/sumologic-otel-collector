receivers:
  riak:
    endpoint: http://localhost:8098
    username: otelu
    password: pass
    collection_interval: 60s
exporters:
  sumologic:
service:
  pipelines:
    logs:
      receivers:
        - riak
      exporters:
        - sumologic
