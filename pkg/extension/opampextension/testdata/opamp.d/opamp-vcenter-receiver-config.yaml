receivers:
  vcenter:
    endpoint: http://localhost:15672
    username: otelu
    password: pass
    collection_interval: 5m
    initial_delay: 1s
exporters:
  sumologic:
service:
  pipelines:
    logs:
      receivers:
        - vcenter
      exporters:
        - sumologic
