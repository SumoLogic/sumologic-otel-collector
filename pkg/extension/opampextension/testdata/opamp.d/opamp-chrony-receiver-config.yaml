receivers:
  chrony:
    endpoint: unix:///dev/null
    timeout: 10s
    collection_interval: 30s
    metrics:
      ntp.skew:
        enabled: true
      ntp.stratum:
        enabled: true
exporters:
  sumologic:
service:
  pipelines:
    logs:
      receivers:
        - chrony
      exporters:
        - sumologic
